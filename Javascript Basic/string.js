const name = "sam" + "vishu"
const repoCount = 50

// console.log(name + repoCount + "Thats it!");

console.log(`Hello my name is ${name} and my repo is ${repoCount}`);

// study about string functions from mdn